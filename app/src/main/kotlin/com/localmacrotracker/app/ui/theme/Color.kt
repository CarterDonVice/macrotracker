package com.localmacrotracker.app.ui.theme

import androidx.compose.ui.graphics.Color

// Backgrounds
val DarkBackground = Color(0xFFF0F7F3)       // soft mint-white page background
val DarkSurface = Color(0xFFFFFFFF)          // pure white card / surface
val DarkSurfaceVariant = Color(0xFFE4EFE9)   // light mint for input fills, progress tracks

// Brand
val AccentGreen = Color(0xFF1B6B45)          // deep forest emerald (primary)
val AccentGreenDim = Color(0xFF145236)       // darker emerald for containers / pressed

// Text
val TextPrimary = Color(0xFF1A2B22)          // near-black with green warmth
val TextSecondary = Color(0xFF5A7266)        // muted sage for secondary labels

// Status / semantic
val ReminderAmber = Color(0xFFB45309)        // dark amber for unsaved badge
val EstimatedColor = Color(0xFF0369A1)       // steel blue for estimated entries
val ErrorRed = Color(0xFFDC2626)             // clear red

// Macro colors
val MacroProtein = Color(0xFF2563EB)         // vibrant blue
val MacroCarbs = Color(0xFFD97706)           // warm amber
val MacroFat = Color(0xFF9333EA)             // rich purple
val MacroCalories = Color(0xFFEA580C)        // warm orange for calorie values

// Structure
val Divider = Color(0xFFD4E5DA)              // soft mint divider
